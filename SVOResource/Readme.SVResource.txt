SVOResource.dll

This dll project has been generated to inlude all the resources needed by the static libraries which cannot include their own resources
The only file needed by other modules using the dll is the resource.h in SVOResource project which contains the resource ID's.

